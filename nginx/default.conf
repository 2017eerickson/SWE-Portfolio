server { 
        listen 80 default_server;
        listen [::]:80 default_server;

        index index.html;

        location / { 
                root /usr/share/nginx/html;
                try_files $uri /index.html; 
                }

                
        }